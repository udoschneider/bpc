#!/bin/bash -x

TEMPDIR=$(mktemp -d)

INFILE_PDF=$TEMPDIR/in.pdf
OUTFILE_PDF=$TEMPDIR/out.pdf

pstopdf "$1" "$2" "$3" "$4" "$5" "$6" > "$INFILE_PDF"
/bpc/overlay "$1" "$2" "$3" "$4" "$5" "$INFILE_PDF" "$OUTFILE_PDF"
pdftops "$1" "$2" "$3" "$4" "$5" "$OUTFILE_PDF"

rm -rf $TEMPDIR

